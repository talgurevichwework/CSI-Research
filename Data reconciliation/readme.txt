File outputs:

Main.py runs the queries.py to spit out a number of CSVs as detailed below: 
output : the output of the comparison of both looker and SF reporting with all accounts and the number gaps
reuse : the reuse records reason and event joined into the output file.
fulloutput : a full right join of the output and the re-use entries
